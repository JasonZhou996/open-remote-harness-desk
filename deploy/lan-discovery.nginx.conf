# Inside the existing HTTPS server block. Inherit its Basic authentication.
location = /api/lan-discovery {
    alias /var/lib/codex-webui/lan-discovery.properties;
    default_type text/plain;
    add_header Cache-Control "no-store";
    add_header X-Content-Type-Options "nosniff";
}
