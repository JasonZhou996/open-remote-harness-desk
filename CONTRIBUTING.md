# 参与开发

先阅读 [README](README.md)，按职责修改 `server/`、`web/` 或 `android/`。本仓库维护自己的产品，不再同步整份上游仓库。

- 引入上游改动时，只带入当前使用的代码和必要依赖，保留来源、版权和许可声明。
- 网页状态应来自真实会话事件；不要用定时器伪造运行、完成或同步。
- 保留协议、鉴权和文件访问边界。不要把服务端凭据、数据库、会话记录、安装包或应用解包内容加入 Git。
- 修正相关导入、部署脚本、锁文件和文档，运行受影响的现有检查；UI 变更需要检查桌面与手机、深浅主题。仓库整理不顺带改样式。
- Claude 服务模块沿用 [组件规则](server/claude/AGENTS.md)。不要为了增加一个功能引入新的服务进程或通用框架。
- 不自动向上游提交或推送。安全问题在维护者的私有沟通渠道报告，见 [SECURITY.md](SECURITY.md)。
